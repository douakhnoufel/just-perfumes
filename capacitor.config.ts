import type { CapacitorConfig } from "@capacitor/cli";

const config: CapacitorConfig = {
  appId: "com.parfumebadou.shop",
  appName: "parfume BADOU",
  webDir: "out",
  server: {
    androidScheme: "https"
  }
};

export default config;
