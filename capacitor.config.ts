import type { CapacitorConfig } from "@capacitor/cli";

const config: CapacitorConfig = {
  appId: "com.justperfum.shop",
  appName: "JUST-PERFUM",
  webDir: "out",
  server: {
    androidScheme: "https"
  }
};

export default config;
